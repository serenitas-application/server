model User {
    id         Int      @id @default(autoincrement())
    email      String   @unique
    username   String
    password   String
    createDate DateTime @default(now()) @map("created_date") @db.Timestamptz(6)

    groups  PageGroup[]
    mood    Mood[]
    diaries Diary[]

    @@map("users")
}
